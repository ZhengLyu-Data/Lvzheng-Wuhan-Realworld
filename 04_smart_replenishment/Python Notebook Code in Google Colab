# 📦 Step 1: Import libraries
import pandas as pd

# 📂 Step 2: Load the raw dataset
df = pd.read_csv("smart_replenishment_sample.csv")

# 🔍 Step 3: Inspect columns and sample data
print("✅ Columns in the dataset:")
print(df.columns.tolist())

print("\n📌 Preview of the data:")
display(df.head())

# 🧹 Step 4: Clean the data (basic operations)
df.drop_duplicates(inplace=True)
df.dropna(inplace=True)

# 🚧 Step 5: Add calculated columns (adjust column names based on the actual dataset)
# 👇 These lines are placeholders and must match your column names
# Example:
# df["StockGap"] = df["ForecastQty"] - df["CurrentStock"]
# df["ShouldReorder"] = df["CurrentStock"] < df["ReorderThreshold"]

# 💾 Step 6: Save the cleaned data
df.to_csv("cleaned_replenishment.csv", index=False)
print("✅ cleaned_replenishment.csv saved successfully")
